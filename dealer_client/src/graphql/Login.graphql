mutation Login($userLogin: UserLogin!) {
  login(login: $userLogin) {
    id
    firstName
    lastName
    email
    username
    dealershipOrganization {
      id
    }
    createdAt
    updatedAt
  }
}
